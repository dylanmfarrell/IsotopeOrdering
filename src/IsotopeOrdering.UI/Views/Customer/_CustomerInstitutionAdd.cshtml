@model CustomerInstitutionDetailModel
<li class="list-group-item add-collection-item">
    <partial name="_Institution" model="new InstitutionDetailModel()" />
    <button type="button" class="btn btn-success" onclick="addCustomerInstitution(this)" data-url="@Url.Action(nameof(CustomerController.AddCustomerInstituion))" data-target="#customer-institution-list">Add New Institution</button>
</li>
