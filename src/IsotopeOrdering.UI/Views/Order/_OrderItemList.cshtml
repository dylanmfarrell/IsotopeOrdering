@model List<OrderItemDetailModel>
    <ul class="list-group" id="order-item-list">
        @for (int i = 0; i < Model.Count; i++) {
            <partial name="_OrderItemAdd" for="@Model[i]" />
        }
    </ul>